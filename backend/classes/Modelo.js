module.exports = class Modelo {
   //Atributos propios
   nombre


   constructor({
      nombre,

   }) {
      this.nombre = nombre

   }


   //metodos 

   getNombre = () => this.nombre
   setNombre = (value) => this.nombre = value










}